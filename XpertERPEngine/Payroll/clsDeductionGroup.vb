Imports System.Data.SqlClient
Imports common
Public Class clsDeductionGroup

#Region "Variables"
    Public Code As String = Nothing
    Public Description As String = Nothing
    Public Created_By As String = Nothing
    Public Created_Date As Date? = Nothing
    Public Modified_By As String = Nothing
    Public Modified_Date As Date? = Nothing
#End Region

    Public Shared Function SaveData(ByVal obj As clsDeductionGroup, ByVal isNewEntry As Boolean) As Boolean
        Dim qry As String = ""
        Dim IsSaved As Boolean = False
        Try
            Dim coll As New Hashtable()
            clsCommon.AddColumnsForChange(coll, "Ded_Description", obj.Description)
            clsCommon.AddColumnsForChange(coll, "Comp_Code", objCommonVar.CurrentCompanyCode)
            clsCommon.AddColumnsForChange(coll, "Modified_By", objCommonVar.CurrentUserCode)
            clsCommon.AddColumnsForChange(coll, "Modified_Date", clsCommon.GetPrintDate(clsCommon.GETSERVERDATE(), "dd/MMM/yyyy hh:mm tt"))
            If isNewEntry Then
                If (IIf(clsCommon.myCstr(clsFixedParameter.GetData(clsFixedParameterType.AllowAutoGenerateDocNoInMaster, clsFixedParameterCode.AllowAutoGenerateDocNoInMaster, Nothing)) = "1", True, False)) Then
                    Dim ChkNewEntry As String = clsDBFuncationality.getSingleValue("Select count(*) from TSPL_DEDUCTION_GROUP where Ded_Code='" & obj.Code & "'")
                    If ChkNewEntry = 0 Then
                        obj.Code = clsERPFuncationality.GetNextCode(Nothing, clsCommon.GetPrintDate(clsCommon.GETSERVERDATE(), "dd/MM/yyyy"), clsDocType.DeductionGroup, "", "")
                        If clsCommon.myLen(obj.Code) <= 0 Then
                            Throw New Exception("Error in Code Generation")
                        End If
                    End If
                End If
                clsCommon.AddColumnsForChange(coll, "Created_By", objCommonVar.CurrentUserCode)
                clsCommon.AddColumnsForChange(coll, "Created_Date", clsCommon.GetPrintDate(clsCommon.GETSERVERDATE(), "dd/MMM/yyyy hh:mm tt"))
                clsCommon.AddColumnsForChange(coll, "Ded_Code", obj.Code)
                IsSaved = clsCommonFunctionality.UpdateDataTable(coll, "TSPL_DEDUCTION_GROUP", OMInsertOrUpdate.Insert, "")
            Else
                IsSaved = clsCommonFunctionality.UpdateDataTable(coll, "TSPL_DEDUCTION_GROUP", OMInsertOrUpdate.Update, "TSPL_DEDUCTION_GROUP.Ded_Code='" + obj.Code + "'")
            End If

        Catch err As Exception
            Throw New Exception(err.Message)
        End Try
        Return IsSaved
    End Function



    Public Shared Function GetData(ByVal strCode As String, ByVal NavType As NavigatorType) As clsDeductionGroup
        Dim obj As clsDeductionGroup = Nothing
        Dim Arr As List(Of clsDeductionGroup) = Nothing
        Dim qry As String = "select Ded_Code ,Ded_Description from TSPL_DEDUCTION_GROUP where 2=2 "
        Dim whrclas As String = ""
        Select Case NavType
            Case NavigatorType.First
                qry += " and TSPL_DEDUCTION_GROUP.Ded_Code = (select MIN(Ded_Code) from TSPL_DEDUCTION_GROUP WHERE 1=1 " + whrclas + " )"
            Case NavigatorType.Last
                qry += " and TSPL_DEDUCTION_GROUP.Ded_Code = (select Max(Ded_Code) from TSPL_DEDUCTION_GROUP WHERE 1=1 " + whrclas + " )"
            Case NavigatorType.Current
                qry += " and TSPL_DEDUCTION_GROUP.Ded_Code = (select TOP 1 Ded_Code from TSPL_DEDUCTION_GROUP WHERE 1=1 " + whrclas + " and Ded_Code='" + strCode + "' )"
            Case NavigatorType.Next
                qry += " and TSPL_DEDUCTION_GROUP.Ded_Code = (select Min(Ded_Code) from TSPL_DEDUCTION_GROUP where Ded_Code > '" + strCode + "' " + whrclas + ")"
            Case NavigatorType.Previous
                qry += " and TSPL_DEDUCTION_GROUP.Ded_Code = (select Max(Ded_Code) from TSPL_DEDUCTION_GROUP where Ded_Code < '" + strCode + "' " + whrclas + ")"
        End Select
        Dim dt As DataTable = clsDBFuncationality.GetDataTable(qry)
        If (dt IsNot Nothing AndAlso dt.Rows.Count > 0) Then
            obj = New clsDeductionGroup()
            obj.Code = clsCommon.myCstr(dt.Rows(0)("Ded_Code"))
            obj.Description = clsCommon.myCstr(dt.Rows(0)("Ded_Description"))
        End If
        Return obj
    End Function

End Class
