Imports common
Imports System.Data.SqlClient
Public Class clsTrainingMaster
#Region "variable"
    Public Code As String = Nothing
    Public Name As String = Nothing
    Public tb_Name As String = Nothing
#End Region
    Public Shared Function SaveData(ByVal obj As clsTrainingMaster, ByVal isnewentry As Boolean)
        Dim trans As SqlTransaction = clsDBFuncationality.GetTransactin()
        Try

            Dim qry As String = "select count(*) from " & obj.tb_Name & " where Code='" + obj.Code + "'"
            Dim isexist As Integer = clsDBFuncationality.getSingleValue(qry, trans)
            If isexist = 0 Then
                isnewentry = True
            Else
                isnewentry = False

            End If
            Dim coll As New Hashtable()
            clsCommon.AddColumnsForChange(coll, "Name", obj.Name)
            clsCommon.AddColumnsForChange(coll, "Comp_Code", objCommonVar.CurrentCompanyCode)
            clsCommon.AddColumnsForChange(coll, "Modified_By", objCommonVar.CurrentUserCode)
            clsCommon.AddColumnsForChange(coll, "Modified_Date", clsCommon.GetPrintDate(clsCommon.GETSERVERDATE(trans), "dd/MMM/yyyy hh:mm tt"))
            clsCommon.AddColumnsForChange(coll, "Posted", "1")
            If isnewentry Then
                If (IIf(clsCommon.myCstr(clsFixedParameter.GetData(clsFixedParameterType.AllowAutoGenerateDocNoInMaster, clsFixedParameterCode.AllowAutoGenerateDocNoInMaster, trans)) = "1", True, False)) Then
                    Dim ChkNewEntry As String = clsDBFuncationality.getSingleValue("Select count(*) from " & obj.tb_Name & " where Code='" + obj.Code + "'", trans)
                    If ChkNewEntry = 0 Then
                        obj.Code = clsERPFuncationality.GetNextCode(trans, clsCommon.GetPrintDate(clsCommon.GETSERVERDATE(trans), "dd/MM/yyyy"), clsDocType.MilkReasonMaster, "", "")
                        If clsCommon.myLen(obj.Code) <= 0 Then
                            Throw New Exception("Error in Code Generation")
                        End If
                    End If
                End If
                clsCommon.AddColumnsForChange(coll, "Code", obj.Code)
                clsCommon.AddColumnsForChange(coll, "Created_By", objCommonVar.CurrentUserCode)
                clsCommon.AddColumnsForChange(coll, "Created_Date", clsCommon.GetPrintDate(clsCommon.GETSERVERDATE(trans), "dd/MMM/yyyy hh:mm tt"))

                clsCommonFunctionality.UpdateDataTable(coll, "" & obj.tb_Name & "", OMInsertOrUpdate.Insert, "", trans)
            Else
                clsCommonFunctionality.UpdateDataTable(coll, "" & obj.tb_Name & " ", OMInsertOrUpdate.Update, " Code='" + obj.Code + "'", trans)
            End If
            trans.Commit()
            Return True
        Catch ex As Exception
            trans.Rollback()
            Throw New Exception(ex.Message)
            Return False
        End Try

    End Function



    Public Function DeleteData(ByVal strcode As String, ByVal str_tb_name As String) As Boolean
        Dim trans As SqlTransaction = clsDBFuncationality.GetTransactin()
        Try
            If (clsCommon.myLen(strcode) >= 0) Then
                Dim qry As String = "delete from " & str_tb_name & " where code='" + strcode + "'"
                clsDBFuncationality.ExecuteNonQuery(qry, trans)
            End If
            trans.Commit()
            Return True
        Catch ex As Exception
            trans.Rollback()
            Throw New Exception(ex.Message)
            Return False
        End Try

    End Function
    Public Shared Function GetData(ByVal code As String, ByVal navigatortype As NavigatorType, ByVal tb_name As String) As clsTrainingMaster
        Try


            Dim obj As clsTrainingMaster = Nothing
            Dim qst As String = ("select Code,Name from " & tb_name & " where 2=2 ")
            Select Case navigatortype
                Case navigatortype.Current
                    qst += "and Code in ('" + code + "')"
                Case navigatortype.Next
                    qst += "and Code in (select  min(Code)from " & tb_name & " where Code >'" + code + "')"
                Case navigatortype.First
                    qst += "and Code in (select MIN(Code)from " & tb_name & ")"

                Case navigatortype.Last
                    qst += "and Code in (select Max(Code)from " & tb_name & ")"
                Case navigatortype.Previous
                    qst += "and Code in (select  max(Code)from " & tb_name & " where Code <'" + code + "')"

            End Select
            Dim dt1 As DataTable = clsDBFuncationality.GetDataTable(qst)
            If dt1 IsNot Nothing AndAlso dt1.Rows.Count > 0 Then
                obj = New clsTrainingMaster
                obj.Code = clsCommon.myCstr(dt1.Rows(0)("Code"))
                obj.Name = clsCommon.myCstr(dt1.Rows(0)("Name"))

            End If
            Return obj
        Catch ex As Exception
            Throw New Exception(ex.Message)
        End Try
    End Function


End Class
